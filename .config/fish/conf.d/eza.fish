# Use 'dim' instead of dark gray for punctuation
# making it work for dark backgrounds
# .. so apparently this is an issue with the 'Horizon'
# theme that I'm using in Ptyxis (other themes correctly adapt
# dark gray to something legible *le sigh*).
set -x EZA_COLORS 'xx=2'
